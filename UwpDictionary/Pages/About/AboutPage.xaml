<Page
    x:Class="UwpDictionary.Pages.About.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpDictionary.Pages.About"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
  >
    <Page.Resources>
        <FontFamily x:Key="AppFont">Suwannaphum</FontFamily>
        <FontFamily x:Key="IconFont">Segoe MDL2 Assets</FontFamily>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock 
        Margin="32,16,32,0"
                       FontSize="22"
                       FontFamily="{StaticResource AppFont}"
            x:Uid="AboutTextBlock" 
               />

        <Border 
                Grid.Row="1"
			Margin="16"
			CornerRadius="8"
			Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid>
                <ScrollViewer HorizontalScrollMode="Disabled" Padding="16">
                    <TextBlock 
                       FontSize="16"
                       FontFamily="{StaticResource AppFont}"
            x:Uid="AboutTextBlock" 
               />
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Page>
